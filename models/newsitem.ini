[model]
name = News Item
label = {{ this.title }}

[fields.date]
label = Date
type = date

[fields.status]
label = Status
type = select
choices = published, expired

[fields.title]
label = Title
type = string

[fields.lead_in]
label = Lead in
type = text

[fields.body]
label = Body
type = markdown
